apiVersion: apps/v1
kind: Deployment
metadata:
  name: games-sqlserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: games-sqlserver
  template:
    metadata:
      labels:
        app: games-sqlserver
    spec:
      containers:
      - name: sqlserver
        image: mcr.microsoft.com/mssql/server:2022-latest
        ports:
        - containerPort: 1433
        env:
        - name: ACCEPT_EULA
          value: "Y"
        - name: SA_PASSWORD
          value: "Games!Passw0rd"
        resources:
          limits:
            memory: "1.5Gi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: games-sqlserver
spec:
  selector:
    app: games-sqlserver
  ports:
  - port: 1433
    targetPort: 1433
